version: 2
branches:
        only:
                - master
jobs:
        build:
                machine: true
                working_directory: ~/k9
                steps:
                        - checkout
                        - run:
                                name: Compile proto files
                                command: |
                                        make compile-protos
                        - run:
                                name: Build scraper container image
                                command: |
                                        make scraper-image
